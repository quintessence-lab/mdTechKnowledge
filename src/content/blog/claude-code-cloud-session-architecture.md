---
title: "Claude Code クラウドセッション構成図"
date: 2026-04-26
updatedDate: 2026-07-14
category: "Claude技術解説"
tags: ["Claude Code", "クラウドアーキテクチャ", "サンドボックス", "GitHub", "AI開発ツール", "Desktop", "Routines"]
excerpt: "claude.ai / code.claude.comからClaude Codeを利用する際のクラウドセッションアーキテクチャを図解。2026年4月14日のDesktopリデザインとRoutines統合情報を反映。"
draft: false
---

**最終更新**: 2026-04-26

claude.ai / code.claude.comからClaude Codeを利用する際のアーキテクチャ図。

> **2026年4月14日のDesktopリデザインについて**: Claude Code Desktopアプリが全面リデザインされ、IDE型ワークスペース（マルチセッションサイドバー、統合ターミナル、Diffビューア、ファイルエディタ等）に刷新されました。下記の構成図はクラウドセッションの基本的な通信フローを示すものであり、Desktop UIの見た目は大きく変わっていますが、サンドボックスコンテナ経由のアーキテクチャ自体は同一です。

```
┌──────────────────────────────────────────────────────────────┐
│                    あなたの操作端末                             │
│                                                              │
│  ┌──────────────┐    ┌──────────────────────────────────┐   │
│  │ ローカル      │    │ ブラウザ                          │   │
│  │ ファイル      │    │                                  │   │
│  │ デスクトップ   │    │  claude.ai / code.claude.com     │   │
│  │ ターミナル    │    │  ┌────────────────────────────┐  │   │
│  │              │    │  │  チャットUI                  │  │   │
│  │  ← × →      │    │  │  「リポジトリとは？」 [送信]   │  │   │
│  │  直接アクセス  │    │  │         ↑ 表示  │ 送信       │  │   │
│  │  不可         │    │  └─────────┼──────┼───────────┘  │   │
│  └──────────────┘    └────────────┼──────┼───────────────┘   │
│         │                         │      │                    │
└─────────┼─────────────────────────┼──────┼────────────────────┘
          │ git pull                │      │
          │ (手動)                  │      │ HTTPS (WebSocket)
          │                         │      │
          │    ┌────────────────────┼──────┼────────────────┐
          │    │   Anthropic クラウド │      │                │
          │    │                    │      ▼                │
          │    │    ┌──────────────────────────────────┐    │
          │    │    │   サンドボックス コンテナ            │    │
          │    │    │                                  │    │
          │    │    │   Claude Code (AI エージェント)    │    │
          │    │    │     ├─ ファイル読み書き             │    │
          │    │    │     ├─ コマンド実行                │    │
          │    │    │     ├─ Git操作                    │    │
          │    │    │     └─ Web検索                    │    │
          │    │    │                                  │    │
          │    │    └──────────────┬───────────────────┘    │
          │    │                  │ git push                │
          │    └──────────────────┼─────────────────────────┘
          │                       │
          │    ┌──────────────────▼─────────────────────┐
          └───→│           GitHub                       │
               │   ユーザーのリポジトリ                    │
               │                                       │
               │   ブランチ: claude/...                   │
               └───────────────────────────────────────┘
```

## ポイント

- ブラウザからAnthropicクラウド上のサンドボックスコンテナに接続（HTTPS/WebSocket）
- Claude Codeはサンドボックス内でファイル操作・Git操作・Web検索を実行
- 成果物はGitHubリポジトリにpush → ローカル端末からgit pullで取得
- ローカルファイルへの直接アクセスは不可

## 2026年4月14日 Desktopリデザインによる変更点

4月14日のリデザインにより、Desktop版（code.claude.com / Mac・Windowsアプリ）のUIがIDE型ワークスペースに刷新された。クラウドセッションの通信アーキテクチャ自体は変更なし。

### Desktop版の新UI構成

```
┌─────────────────────────────────────────────────────────────┐
│  Claude Code Desktop (IDE型ワークスペース)                     │
│                                                             │
│  ┌──────────┐ ┌────────────────────────────────────────┐   │
│  │サイドバー  │ │  メインエリア                            │   │
│  │          │ │                                        │   │
│  │ セッション │ │  ┌──────────┐ ┌──────────┐            │   │
│  │ 一覧      │ │  │ チャット   │ │ Diff     │            │   │
│  │          │ │  │          │ │ ビューア  │            │   │
│  │ ステータス │ │  └──────────┘ └──────────┘            │   │
│  │ フィルタ  │ │  ┌──────────┐ ┌──────────┐            │   │
│  │          │ │  │ 統合      │ │ プレビュー │            │   │
│  │          │ │  │ ターミナル │ │ ペイン    │            │   │
│  └──────────┘ │  └──────────┘ └──────────┘            │   │
│               │  ※ ドラッグ&ドロップで自由に配置変更可能    │   │
│               └────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
         │ HTTPS (WebSocket)
         ▼
  Anthropicクラウド → サンドボックスコンテナ → GitHub
  （通信アーキテクチャは従来と同一）
```

### 主な新機能

| 機能 | 説明 |
|------|------|
| **マルチセッションサイドバー** | 全アクティブ・最近のセッションを一覧管理。複数リポジトリを並行操作可能 |
| **統合ターミナル** | セッションの作業ディレクトリで直接コマンド実行 |
| **再構築Diffビューア** | 大規模な変更セットでもパフォーマンス向上 |
| **ドラッグ&ドロップレイアウト** | ターミナル、プレビュー、Diff、チャットを自由に配置 |
| **ファイルエディタ** | チャットやDiffからファイルパスをクリックして直接編集 |
| **プレビューペイン** | HTML、PDF、画像をアプリ内表示 |
| **サイドチャット**（Cmd+;） | 実行中タスクから本スレッドに影響を与えず質問を分岐 |
| **3つの表示モード** | Verbose / Normal / Summary でツール呼び出しの詳細度を選択 |
| **Routines統合** | リデザインされたDesktopアプリからRoutines（スケジュール/イベント駆動のクラウド自動実行）をGUI操作可能。マルチセッションサイドバーから複数Routinesを同時起動・監視 |

### CLI版との違い

- **CLI版**（`claude`コマンド）: 今回のリデザインの対象外。従来通りターミナルで動作
- **Desktop版**: IDE型ワークスペースに全面刷新。GUI特有の機能（サイドバー、ファイルエディタ等）が追加

## Routinesとクラウドセッションの関係

2026年4月15日に同時リリースされた **Routines**（スケジュール/API/GitHubイベント駆動の自動実行機能）も、本記事と同じクラウドセッション・サンドボックスコンテナ上で動作する。

```
       ┌──────────────────┐
       │ トリガー         │
       │ ・cronスケジュール│
       │ ・API呼び出し    │
       │ ・GitHubイベント │
       └────────┬─────────┘
                ▼
   Anthropicクラウド → サンドボックスコンテナ → GitHub
   （通常のクラウドセッションと同一アーキテクチャ）
                │
                ▼
       Desktopアプリ サイドバーに
       実行ログ・成果物を表示
```

- **対話セッション** = 人がチャットでリアルタイム指示
- **Routines** = 人不在でもトリガーから自動実行（バックグラウンド実行）
- 両者ともサンドボックスコンテナ経由で同じ通信アーキテクチャを共有
- Desktop版マルチセッションサイドバーから両者を横断管理可能

## 【2026-07-04追記】Claude apps gateway — 企業向けセルフホスト型コントロールプレーン（2026-06-29）

2026年6月29日、企業が Claude Code を自己ホストで統制するための **Claude apps gateway** が発表されました（Amazon Bedrock / Google Cloud 向け）。上記の「Anthropic ホストのクラウドセッション」とは別に、**自社インフラ側に制御レイヤーを1つ挟む**構成です。

| 要素 | 内容 |
|:---|:---|
| 構成 | Linux 上の**単一ステートレスコンテナ**＋ PostgreSQL。`claude` バイナリに統合 |
| 認証 | OIDC リライングパーティ（Google Workspace / Microsoft Entra ID / Okta ほか OIDC 準拠）で短期セッション発行 |
| ポリシー | 中央で一度定義 → クライアントへ自動配布・適用 |
| アクセス制御 | ロールベースアクセス制御（RBAC） |
| コスト帰属 | ユーザー単位の利用追跡・支出レポート |
| 推論ルーティング | Claude API / Amazon Bedrock / Google Cloud へルーティング＋プロバイダ間フェイルオーバー |
| 支出上限 | 日次 / 週次 / 月次を組織・グループ・ユーザー単位で設定 |

「クラウドセッション（Anthropic ホスト）」に対し、apps gateway は**自社が SSO・ポリシー・課金を握る自己ホスト型**という位置づけです。出典: [Claude apps gateway 公式](https://claude.com/blog/introducing-the-claude-apps-gateway)。

## 【2026-07追記】Claude Desktop for Linux（ベータ）

2026年6月30日、**Claude Desktop の Linux版が公式ベータ**として公開されました。**Ubuntu 22.04 以降・Debian 12 以降**（x86_64・arm64）が対象で、Anthropic 公式 apt リポジトリからインストールします。macOS/Windows と同じ Chat・Cowork・Claude Code 体験（並列セッション・ビジュアル diff レビュー・統合ターミナル/エディタ）を提供しますが、**Computer Use・音声入力は未対応**（Fedora/RHEL/Arch も現時点は非対応）。詳細は [Claude Codeの実行環境まとめ](/mdTechKnowledge/blog/claude-code-execution-environments/) を参照。出典: [Claude Desktop on Linux（公式）](https://code.claude.com/docs/en/desktop-linux)。
